fileVersion: 1
id: "388"
isDisabled: false
metadata:
  defaultStorageLocation: null
  error: null
  nodeMetadataSpec: |-
    capitalized: Dimension Advanced Deploy
    short: dim
    tagColor: '#1E339A'
    plural: Dimensions

    deployStrategy: advanced

    config:
    - groupName: Create Options
      items:
      - type: materializationSelector
        isRequired: true
        default: table
        options:
        - table
        - view

      - type: overrideSQLToggle
        enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"

      - type: toggleButton
        displayName: Primary Key
        default: false
        attributeName: primary
        enableIf: "{% if node.materializationType != 'view' %} true {% else %} false {% endif %}"

      - type: tabular   
        displayName: Primary Key Columns*
        attributeName: primaryKeyConfig
        columns:

        -  type: columnDropdownSelector
           displayName: Primary Key Columns
           attributeName: primaryKey

        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.primary %} true {% else %} false {% endif %}"

      - type: toggleButton
        attributeName: partitionBy
        displayName: Enable Partitioning
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' %} true {% else %} false {% endif %}"

      - displayName: Partition By*
        attributeName: partByColumnConfig
        type: dropdownSelector
        default: "Time-Unit Column"
        options:
        - "Time-Unit Column"
        - "Integer Range"
        - "Ingestion Time"
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: "Time-Unit Expression (Ex: DATE_TRUNC(columnName, MONTH))"
        attributeName: partByTimeSqlExpression
        default: ""
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and config.partByColumnConfig == 'Time-Unit Column' %} true {% else %} false {% endif %}"

      - type: label
        displayName: -- OR --
        attributeName: labelOR
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and config.partByColumnConfig == 'Time-Unit Column' %} true {% else %} false {% endif %}"

      - type: columnDropdownSelector
        displayName: Partition By Column (dataType-DATE)
        attributeName: partByColumn
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and config.partByColumnConfig == 'Time-Unit Column' %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: "Integer Range Expression* (Ex: RANGE_BUCKET(columnName, GENERATE_ARRAY(1, 100, 200)))"
        attributeName: partByIntSqlExpression
        default: ""
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and config.partByColumnConfig == 'Integer Range' %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: Ingestion-time Expression(Optional) (Ex. DATE_TRUNC(_PARTITIONTIME, MONTH))
        attributeName: partByIngnTimeExpression
        default: ''
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and config.partByColumnConfig == 'Ingestion Time' %} true {% else %} false {% endif %}"

      - type: toggleButton
        attributeName: partitionFilter
        displayName: Require Partition Filter
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: Partition Expiration Days(Optional) (Ex. 30)
        attributeName: partExpireDays
        default: ''
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy and ( config.partByColumnConfig == 'Time-Unit Column' or config.partByColumnConfig == 'Ingestion Time' ) %} true {% else %} false {% endif %}"

      - type: toggleButton
        attributeName: clusterKey
        displayName: Enable Clustering
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.partitionBy %} true {% else %} false {% endif %}"

      - type: tabular
        displayName: Cluster By* (Only first 4 rows are processed)
        attributeName: clusterKeyConfig
        columns:

        -  type: columnDropdownSelector
           displayName: Cluster By Columns
           attributeName: clustColumn
           isRequired: false
      
        isRequired: false
        enableIf: "{% if node.materializationType != view and config.clusterKey %} true {% else %} false {% endif %}"

      - type: toggleButton
        attributeName: tabExpiration
        displayName: Table Expiration
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Expiration Type*
        attributeName: exprnType
        default: "DAYS FROM NOW"
        type: dropdownSelector
        options:
        - "EXACT DATE/DATETIME"
        - "DAYS FROM NOW"
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.tabExpiration %} true {% else %} flase {% endif %}"

      - type: textBox
        displayName: 'Expiration Value* (Ex: 2024-12-31 | 2024-12-31 23:59:59)'
        attributeName: timeExprnExpression
        default: ""
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.tabExpiration and config.exprnType == 'EXACT DATE/DATETIME' %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: 'Expiration Value* (Ex: 30)'
        attributeName: dayExprnExpression
        default: ""
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.tabExpiration and config.exprnType == 'DAYS FROM NOW' %} true {% else %} false {% endif %}"

      - displayName: Default Rounding Mode(Optional)
        attributeName: defRndMode
        default: ""
        type: dropdownSelector
        options:
        - ""
        - "ROUND_HALF_AWAY_FROM_ZERO"
        - "ROUND_HALF_EVEN"
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' %} true {% else %} flase {% endif %}"
        
    - groupName: Load Dimension Options

      items:

      - type: multisourceToggle
        enableIf: "{% if node.materializationType == 'view' and node.override.create.enabled %} false {% else %} true {% endif %}"

      - displayName: Multi Source Strategy*
        attributeName: insertStrategy
        type: dropdownSelector
        default: UNION ALL
        options:
        - "UNION DISTINCT"
        - "UNION ALL"
        isRequired: false
        enableIf: "{% if not(node.materializationType == 'view' and node.override.create.enabled) and node.isMultisource %} true {% else %} false {% endif %}"

      - type: businessKeyColumns
        isRequired: true

      - type: changeTrackingColumns (If chosen, treats as SCD2)
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Truncate Before
        attributeName: truncateBefore
        type: toggleButton
        default: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Distinct
        attributeName: selectDistinct
        type: toggleButton
        enableIf: "{% if config.groupByAll or (node.materializationType == 'view' and node.override.create.enabled) %} false {% else %} true {% endif %}"
        default: false
        
      - displayName: Group By All
        attributeName: groupByAll
        type: toggleButton
        default: false
        enableIf: "{% if config.selectDistinct or (node.materializationType == 'view' and node.override.create.enabled) %} false {% else %} true {% endif %}"

    - groupName: Other Options
      items:

      - displayName: Enable Tests
        attributeName: testsEnabled
        type: toggleButton
        default: false

      - displayName: Pre-SQL (Optional)
        attributeName: preSQL
        type: textBox
        syntax: sql
        isRequired: false

      - displayName: Post-SQL (Optional)
        attributeName: postSQL
        type: textBox
        syntax: sql
        isRequired: false

      isRequired: false
      enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

    systemColumns:
    - displayName: '{{NODE_NAME}}_KEY'
      transform: ''
      dataType: STRING
      placement: beginning
      attributeName: isSurrogateKey

    - displayName: system_version
      transform: ''
      dataType: INT64
      placement: end
      attributeName: isSystemVersion

    - displayName: system_current_flag
      transform: ''
      dataType: STRING
      placement: end
      attributeName: isSystemCurrentFlag

    - displayName: system_start_date
      transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemStartDate

    - displayName: system_end_date
      transform: CAST('2999-12-31 00:00:00' AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemEndDate

    - displayName: system_create_date
      transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemCreateDate

    - displayName: system_update_date
      transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemUpdateDate
name: Dimension Advanced Deploy
type: NodeType
