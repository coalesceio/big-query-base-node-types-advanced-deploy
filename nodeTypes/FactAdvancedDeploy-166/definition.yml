fileVersion: 1
id: "389"
isDisabled: false
metadata:
  defaultStorageLocation: null
  error: null
  nodeMetadataSpec: |-
    capitalized: Fact Advanced Deploy
    plural: Facts
    short: FCT
    tagColor: '#D9A438'

    deployStrategy: advanced

    config:
    - groupName: Options
      items:
      - type: materializationSelector
        isRequired: true
        default: table
        options:
        - table
        - view
        - transient table

      - type: overrideSQLToggle
        enableIf: "{% if node.materializationType == 'view' %} true {% else %} false {% endif %}"

      - type: multisourceToggle
        enableIf: "{% if node.materializationType == 'view' and node.override.create.enabled %} false {% else %} true {% endif %}"

      - displayName: Multi Source Strategy
        attributeName: insertStrategy
        type: dropdownSelector
        default: UNION ALL
        options:
        - "UNION"
        - "UNION ALL"
        isRequired: false
        enableIf: "{% if not(node.materializationType == 'view' and node.override.create.enabled) and node.isMultisource %} true {% else %} false {% endif %}"

      - type: toggleButton
        displayName: Primary Key
        attributeName: primary
        default: false
        enableIf: "{% if node.materializationType != 'view' %} true {% else %} false {% endif %}"

      - type: textBox
        displayName: Primary Key Name
        attributeName: primaryKeyName
        enableIf: "{% if node.materializationType != 'view' and config.primary %} true {% else %} false {% endif %}"

      - type: tabular   
        displayName: Primary Key Columns
        attributeName: primaryKeyCol
        columns:

        -  type: columnDropdownSelector
           displayName: Primary Key Columns
           attributeName: primaryKey

        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.primary %} true {% else %} false {% endif %}"

      - type: businessKeyColumns
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Unmatched Record Strategy
        attributeName: deleteStrategy
        type: dropdownSelector
        default: "NO DELETE"
        options:
        - "NO DELETE"
        - "HARD DELETE"
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' 
                      and node.isMultisource == false
                      and (columns 
                          | selectattr('isBusinessKey')
                          | list
                          | length > 0) %} true {% else %} false {% endif %}"

      - displayName: Exclude Columns from Merge (For SCD Type I)
        attributeName: excludeMergeToggle
        type: toggleButton
        default: false
        enableIf: "{% if node.materializationType != 'view' and (columns 
                      | selectattr('isBusinessKey')
                      | list
                      | length > 0) %} true {% else %} false {% endif %}"

      - type: tabular
        displayName: 'Exclude Merge List'
        attributeName: excludeMergeList
        columns:

          -  type: columnDropdownSelector
             displayName: Exclude Column Name
             attributeName: excludeColName
           
        isRequired: false
        enableIf: "{% if config.excludeMergeToggle
                      and node.materializationType != 'view' 
                      and node.isMultisource == false
                      and (columns 
                          | selectattr('isBusinessKey')
                          | list
                          | length > 0) %} true {% else %} false {% endif %}"

      - type: toggleButton
        attributeName: clusterKey
        displayName: Cluster Key
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - type: toggleButton
        attributeName: clusterKeyExpressions
        displayName: Allow Expressions in Cluster Key
        default: false
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.clusterKey %} true {% else %} false {% endif %}"

      - type: tabular
        displayName: 'Cluster Key'
        attributeName: clusterKeyConfig
        columns:

        -  type: columnDropdownSelector
           displayName: Column Name
           attributeName: columnName
           isRequired: false
      
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.clusterKey and not config.clusterKeyExpressions %} true {% else %} false {% endif %}"

      - type: tabular
        displayName: 'Cluster Key'
        attributeName: clusterKeyConfigExpressions
        columns:

        -  type: columnDropdownSelector
           displayName: Column Name
           attributeName: columnNameExpressions
           isRequired: false
      
        -  type: textBox
           displayName: Expression(Column name to be enclosed within double quotes)
           attributeName: sqlExpression
           default: "ex. trunc(\"<column_name>\", -5)"
           isRequired: false
      
        isRequired: false
        enableIf: "{% if node.materializationType != 'view' and config.clusterKey and config.clusterKeyExpressions %} true {% else %} false {% endif %}"

      - displayName: Truncate Before
        attributeName: truncateBefore
        type: toggleButton
        default: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Enable Tests
        attributeName: testsEnabled
        type: toggleButton
        default: false
        
      - displayName: Distinct
        attributeName: selectDistinct
        type: toggleButton
        default: false
        enableIf: "{% if config.groupByAll or (node.materializationType == 'view' and node.override.create.enabled) %} false {% else %} true {% endif %}"

      - displayName: Group By All
        attributeName: groupByAll
        type: toggleButton
        default: false
        enableIf: "{% if config.selectDistinct or (node.materializationType == 'view' and node.override.create.enabled) %} false {% else %} true {% endif %}"

      - displayName: Order By
        attributeName: orderby
        type: toggleButton
        default: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - type: tabular
        displayName: 'Sort column'
        attributeName: orderbycolumn
        columns:

          -  type: columnDropdownSelector
             displayName: Column Name
             attributeName: sortColName

          -  type: dropdownSelector
             displayName: Sort Order
             attributeName: sortOrder
             default: desc
             options:
             - desc
             - asc
           
        isRequired: true
        enableIf: "{% if node.materializationType != 'view' and config.orderby %} true {% else %} false {% endif %}"

      - displayName: Pre-SQL
        attributeName: preSQL
        type: textBox
        syntax: sql
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

      - displayName: Post-SQL
        attributeName: postSQL
        type: textBox
        syntax: sql
        isRequired: false
        enableIf: "{% if node.materializationType == 'view' %} false {% else %} true {% endif %}"

    systemColumns:
    - displayName: SYSTEM_CREATE_DATE
      transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemCreateDate

    - displayName: SYSTEM_UPDATE_DATE
      transform: CAST(CURRENT_TIMESTAMP AS TIMESTAMP)
      dataType: TIMESTAMP
      placement: end
      attributeName: isSystemUpdateDate
name: Fact Advanced Deploy
type: NodeType
